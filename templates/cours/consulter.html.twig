{% extends 'base.html.twig' %}

{% block title "Consulter le cours" ~ cours.id %}

{% block body %}

	<h3>
		{{ cours.titre }}
		-
		<a href="{{ path('app_cours_editer', {'id' : cours.id }) }}">
			Editer
		</a>
	</h3>
	<p>{{ classeDeCours.description }}</p>
	<br><br>


	<a href="{{ path('app_classedecours_index') }}">Revenir à ma classe</a>

{% endblock %}
