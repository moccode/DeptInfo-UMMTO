{{ form_start(formPromotion) }}
{{ form_row(formPromotion.promotion) }}
<div class="d-grid gap-2 mt-3">
	<button class="btn btn-primary" type="submit">
		Modifier
	</button>
</div>
{{ form_end(formPromotion) }}
